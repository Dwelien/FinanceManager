import React from 'react';
import { View } from 'react-native';
import LoginPage from './src/components/LoginPage'; // Import the LoginPage component

const App = () => {
  return (
    <View style={{ flex: 1 }}>
      <LoginPage />
    </View>
  );
};

export default App;
